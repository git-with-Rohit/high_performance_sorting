#ifndef UTILS_H
#define UTILS_H

#include <vector>
#include <iostream>

/**
 * Prints the elements of the given vector to the standard output.
 * @param data The vector to be printed.
 */
void print_vector(const std::vector<int>& data);

#endif // UTILS_H
