#ifndef RADIX_SORT_H
#define RADIX_SORT_H

#include <vector>

/**
 * Sorts the given vector of integers using the Radix Sort algorithm.
 * @param arr The vector of integers to be sorted.
 */
void radix_sort(std::vector<int>& arr);

#endif // RADIX_SORT_H
